# Power BI

O arquivo `.pbix` principal será mantido nesta pasta quando a equipe concluir o
MVP. Não registre wallets, senhas, credenciais ou arquivos de configuração do
Oracle neste diretório.

Views esperadas no esquema `ADMIN`:

- `VW_INTERNACOES_DASHBOARD` — 3.924 linhas;
- `VW_INTERNACOES_MENSAIS` — 12 linhas;
- `VW_RANKING_MUNICIPIOS` — 327 linhas.

Consulte `docs/Guia_Conexao_Power_BI_Oracle_Enterprise_Challenge.docx`.
